version: '3.8'

services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-obsidian_comments}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-obsidian_prod_db_2025_secure}
    # No port exposure - only accessible within Docker network
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - obsidian_prod_network

  redis:
    image: redis:7-alpine
    # No port exposure - only accessible within Docker network
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - obsidian_prod_network

  backend:
    build:
      context: ./packages/backend
      dockerfile: Dockerfile.production
    image: ghcr.io/pangeafate/obsidiancomments-backend:latest
    container_name: obsidian-backend
    expose:
      - "8081"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-obsidian_comments}
      - REDIS_URL=redis://redis:6379
      - PORT=8081
      - FRONTEND_URL=${FRONTEND_URL:-http://obsidiancomments.serverado.app}
      - CORS_ORIGIN=${CORS_ORIGIN:-https://obsidiancomments.serverado.app}
      - JWT_SECRET=${JWT_SECRET:-obsidian_jwt_secret_2025_production}
      - RATE_LIMIT_WINDOW=${RATE_LIMIT_WINDOW:-900000}
      - RATE_LIMIT_MAX=${RATE_LIMIT_MAX:-100}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - obsidian_prod_network
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:8081/api/health', (res) => process.exit(res.statusCode === 200 ? 0 : 1))"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  hocuspocus:
    build:
      context: ./packages/hocuspocus
      dockerfile: Dockerfile.production
    image: ghcr.io/pangeafate/obsidiancomments-hocuspocus:latest
    container_name: obsidian-hocuspocus
    expose:
      - "8082"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-obsidian_comments}
      - REDIS_URL=redis://redis:6379
      - PORT=8082
      - CORS_ORIGIN=${CORS_ORIGIN:-https://obsidiancomments.serverado.app}
    depends_on:
      - postgres
      - redis
      - backend
    restart: unless-stopped
    networks:
      - obsidian_prod_network
    # Use built-in Docker health check from Dockerfile instead of overriding

  frontend:
    build:
      context: ./packages/frontend
      dockerfile: Dockerfile.production
      args:
        VITE_API_URL: https://obsidiancomments.serverado.app
        VITE_WS_URL: wss://obsidiancomments.serverado.app/ws
    image: ghcr.io/pangeafate/obsidiancomments-frontend:latest
    container_name: obsidian-frontend
    expose:
      - "80"
    restart: unless-stopped
    networks:
      - obsidian_prod_network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx-fixed.conf:/etc/nginx/nginx.conf:ro
      - nginx_logs:/var/log/nginx
    depends_on:
      - frontend
    restart: unless-stopped
    networks:
      - obsidian_prod_network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s

volumes:
  postgres_data:
  redis_data:
  nginx_logs:

networks:
  obsidian_prod_network:
    driver: bridge