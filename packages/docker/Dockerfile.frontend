# Production stage - use pre-built assets
FROM nginx:alpine

# Copy pre-built assets to nginx
COPY packages/frontend/dist /usr/share/nginx/html

# Copy nginx configuration
COPY packages/docker/nginx-frontend.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]