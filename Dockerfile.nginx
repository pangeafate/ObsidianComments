FROM nginx:alpine

# Install curl for health checks
RUN apk add --no-cache curl

# Copy nginx configuration is handled by volume mount in docker-compose

EXPOSE 80 443

CMD ["nginx", "-g", "daemon off;"]